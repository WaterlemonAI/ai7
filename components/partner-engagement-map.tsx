'use client'
import { useState } from 'react'
import { ArrowRight } from 'lucide-react'
export function PartnerEngagementMap({stages}:{stages:readonly {label:string;title:string;copy:string}[]}){const[active,setActive]=useState(0);const stage=stages[active];return <div className="grid gap-6 lg:grid-cols-[0.85fr_1.15fr]"><div className="space-y-2" role="tablist" aria-label="Engagement stages">{stages.map((item,index)=><button key={item.label} type="button" role="tab" aria-selected={active===index} onClick={()=>setActive(index)} className={`group flex w-full items-center justify-between rounded-2xl border px-5 py-4 text-left transition-all ${active===index?'border-primary bg-primary text-primary-foreground shadow-lg shadow-primary/15':'border-border bg-background hover:border-primary/30 hover:bg-card'}`}><span><span className={`font-mono text-xs uppercase tracking-[0.16em] ${active===index?'text-primary-foreground/70':'text-primary'}`}>{item.label}</span><span className="mt-1 block font-medium">{item.title}</span></span><ArrowRight className={`h-4 w-4 shrink-0 transition-transform ${active===index?'translate-x-1':''}`}/></button>)}</div><div className="relative min-h-[340px] overflow-hidden rounded-3xl border border-primary/15 bg-[#07172b] p-8 text-white sm:p-10"><div className="absolute -right-16 -top-16 h-56 w-56 rounded-full border border-blue-400/20 bg-blue-500/10 blur-sm"/><div className="absolute bottom-8 right-8 grid grid-cols-5 gap-2 opacity-35">{Array.from({length:20}).map((_,i)=><span key={i} className="h-1.5 w-1.5 rounded-full bg-blue-300"/>)}</div><p className="relative font-mono text-xs uppercase tracking-[0.2em] text-blue-300">Current stage</p><p className="relative mt-8 text-6xl font-semibold text-blue-300">{String(active+1).padStart(2,'0')}</p><h3 className="relative mt-6 max-w-xl text-3xl font-semibold tracking-tight">{stage.title}</h3><p className="relative mt-5 max-w-xl text-lg leading-8 text-blue-100/75">{stage.copy}</p></div></div>}
